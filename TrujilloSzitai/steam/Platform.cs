namespace steam {
    class Platform {
        string _name = "";
        List<Product> _products = new List<Product>();
        List<User> _users = new List<User>();

        public Platform() {
            
        }
    }
}